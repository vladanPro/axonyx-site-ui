services:
  - type: web
    name: axonyx-site-ui
    runtime: rust
    plan: free
    buildCommand: cargo install cargo-axonyx --version 0.1.94 --force && cargo ax build --clean
    startCommand: cargo ax run start --host 0.0.0.0 --port $PORT
    healthCheckPath: /__axonyx/health
